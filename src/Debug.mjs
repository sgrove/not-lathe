// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Belt_Option from "rescript/lib/es6/belt_Option.js";
import * as Caml_option from "rescript/lib/es6/caml_option.js";

function assignToWindowForDeveloperDebug(_name, _value) {
  var match = typeof window === "undefined" ? undefined : window;
  if (match !== undefined) {
    return (window[_name] = _value);
  }
  
}

var $$JSON = {};

function stringify(a) {
  return ((function stringifyRelayData(data) {
  const helper = (oldObj) => {
    let obj;
    if (oldObj && typeof oldObj === "object") {
      obj = {};

      var allKeys = Object.keys(oldObj);
      for (var i = 0; i < allKeys.length; i++) {
        var k = allKeys[i];

        const isBadKey = k.startsWith("__")

        if (!isBadKey) {
          var value = oldObj[k];

          if (Array.isArray(value)) {
            value = value.map(helper);
          } else if (typeof value === "object") {
            value = helper(value);
          }

          obj[k] = value;
        }
      }
    }
    return obj ?? oldObj;
  };

  return JSON.stringify(helper(data), null, 2);
}))(a);
}

var Relay = {
  stringify: stringify
};

function appVersion() {
  return Belt_Option.map(Caml_option.undefined_to_opt(typeof navigator === "undefined" ? undefined : navigator), (function ($$navigator) {
                return $$navigator.appVersion;
              }));
}

function isChrome(param) {
  return Belt_Option.mapWithDefault(appVersion(), false, (function (version) {
                return version.indexOf("Chrome") > -1;
              }));
}

var $$Navigator = {
  appVersion: appVersion,
  isChrome: isChrome
};

export {
  assignToWindowForDeveloperDebug ,
  $$JSON ,
  Relay ,
  $$Navigator ,
  
}
/* No side effect */
